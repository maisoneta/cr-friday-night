# Copy this file to .env and fill in your values.
# Never commit .env to git. See README for setup instructions.

# MONGO_URI - Required. Get from MongoDB Atlas (Connect > Drivers) or use a local URI.
# Do not paste real credentials here; this file may be committed.

# PORT - Optional, defaults to 5002
# PORT=5002

# ALLOWED_ORIGINS - Optional. Comma-separated CORS origins.
# Example: https://your-app.onrender.com,http://localhost:3000

# Resend (API-based email; works on Render free tier - no SMTP)
# Get API key at resend.com. Free tier: 100 emails/day.
# RESEND_API_KEY - Required for email. From Resend dashboard > API Keys.
# RESEND_FROM - Sender address. Free tier: "CR Friday Night <onboarding@resend.dev>" (sends only to your signup email).
#               Production: verify a domain and use e.g. "CR <notifications@yourdomain.com>"
# EMAIL_TO - Recipient(s) for report emails. Single or comma-separated.
# Example: person1@example.com,person2@example.com,person3@example.com,person4@example.com
